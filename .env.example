# Frontend environment variables (optional)
# Copy to .env.local for local overrides

# API base URL (default: /api via Vite proxy)
VITE_API_URL=/api

# Force mock/demo mode without backend (true/false)
VITE_USE_MOCK=false
